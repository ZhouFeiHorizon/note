##  记事提醒

- 待提醒列表分析，
- 对记事提醒里的核心的函数进行分析，了解大致功能
- 对elementUI表格条纹样式应用及讲解
- 关于元素垂直居中问题进行讨论



## 开发三部部门会议 

2019/12/20 周五 

- 对部门员工本周所做工作及下周工作安排进行询问了解
- 告诉年终面谈记录表说明，对这个表有什么疑问的进行询问  
-  需要写一个大纲，下一个周一一沟通，全员参加
- 准备下，每个人大概半小时



## 

- Chrome浏览器
  - 程序调试 （单步调试，逐语句，下一个断点，跳出函数）
  - 地址栏搜索

- `package.json` 里的 `script` 字段说明
  - 配置环境变量 `cross-env`
  - `npm run 字段名`  执行了那些

- 程序命名规范

  - 哪一种命名方式好

    |   修饰词在前   |   修饰词在后   |
    | :------------: | :------------: |
    |   showDialog   |   dialogShow   |
    | changePageSize | pageSizeChange |
    |                |                |
    |                |                |

- 时间格式化讲解

- 节流和防抖

 

# 

2020/1/9
主题：构造函数之继承
分享人：王小绍

内容：
通过一个时钟示例来介绍js的继承
  实例方法和静态方法的区别
1. es5继承、通过原型链继承
2. es6继承、es6继承通过extends、constructor 继承是否要使用 super 关键字， 当没有constructor时会默认调用父类的 super，而重写constructor 是第一行必须调用 super 关键字使this指向父类

图片轮播-用继承来实现
1. $.extend 方法
2.  setInterval函数问题，第二个参数时间间隔不写时，间隔会很小，约小于16ms， 执行时间，不会立即执行
3. shift() 方法用于把数组的第一个元素从其中删除，并返回第一个元素的值，unshift() 方法可向数组的开头添加一个或更多元素，并返回新的长度。
4. bind、call、apply的区别，bind 只是绑定this执向，并不会立即执行该函数，而call、apply会改变this执向，并且会立即执行该函数。





内容 
    1.  chrome调试工具的使用  1.1 断点  1.2 调试（有些是语句调试，不进入函数内部，有些是分步调试，会进入函数内部）  1.3 禁用断点调试：disable all breakpoints  1.4 时间绑定-》查询事件所在的js文件  1.5 怎么在元素子节点和属性被修改时打断点  
        2.  地址栏快捷搜索  2.1 怎么添加搜索引擎-〉chrome-》高级设置=〉添加搜索引擎；  2.2 在地址栏输入关键字：按tab键就可进入相应的搜索引擎，进入相关搜索；  
            3.  package.json  3.1 在package.json的script脚本中，定义的脚本，直接在控制台输入无法执行，可以使用npx，他会在执行npx的时候，默认增加环境；  3.2 repository的含义，对应在npm上显示的位置；  3.3 description描述，对应在npm上显示的位置；  3.4 依赖包中版本号的含义  
                4.  如何创建一个cli  
                    5.  命名那些事  5.1 js函数命名  5.2 css命名：BEM（block element modifier）命名，不要嵌套太多（参考，可不做要求，团队内部还是以短线连接）  
                        6.  一些相关的小tip  6.1 dialog显示隐藏的处理方式，采用this.$ref.dialog.doSomething；  6.2 js 字符串的replace方法；  6.3 时间格式化方法 @周飞，把分享出来；  
                            7.  防抖和节流（下次分享）

