

# 2020/01/02前端小组会议

### Chrome浏览器应用

**调试**

>Breakpoints: 断点
>
>Sources（源码）
>
>Event Listener Breakpoints（事件监听断点）
>
>Call Stack（函数调用栈）

- 打断点方式 
  - chrome `sources` 面板中打断点
  - 程序文件中写  `debugge`

- 调试
  - 跳到下一个断点位置(F8), 没有断点是全部执行完
  - `逐过程调试`(F10)，`逐过程`:就是把一个函数当成一条语句，不进入函数内部
  - `逐语句调试` (F11)，`逐语句`: 进入函数内部，调试窗口会进入这个函数，然后程序断点会跳转到这个函数里的第一个语句。
  - `跳出当前函数`(shift+F11)
  - `下一步`(F9)和逐语句调试一样

- 监听DOM元素执行了那些事件
  - 通过 `elements` 面板中的 `Event Listeners` 来监听
  - 通过 `souces` 面板中的 `Event Listener Breakpoints` 里面的来监听

**快捷搜索**

1. 地址栏搜索

### package.json文件说明

1. `name 包名`

2. `version 版本号`

3. `description 项目描述信息`

4. `author 作者`

5. `private 是否私有`

6. `scripts 脚本命令`  `npm run 字段名` 执行脚本

   - 配置环境变量 `cross-env`
   - `npm run 字段名`  执行了那些

7. `dependencies 开发环境和生成环境都会依赖的包`

8. `devDependencies 开发环境依赖的包`

9. `dependencies` 和 `devDependencies ` 包版本说明
   - 指定版本： 如：“2.2.5”，表示安装2.2.5的版本
   - ~  如：“~1.1.0” ,表示安装1.1.x的最新版本（不低于1.1.0），但是不安装1.2.x，也就是说安装时不改变大版本号和次要版本号
   - ^  如： “^3.1.4”,，表示安装3.1.4及以上的版本，但是不安装4.0.0，也就是说安装时不改变大版本号

10. `keywords 关键字，进行npm搜索包时能快速定位`

11. `repository 仓库地址`

12. `browserslist 目标浏览器配置表` 提供的目标浏览器的环境来，智能添加css前缀，js的polyfill垫片,来兼容旧版本浏览器，而不是一股脑的添加。避免不必要的兼容代码，以提高代码的编译质量。

13. `bin `

    `#!/usr/bin/env node`

    `npm link`

    `npm 全局安装包的位置 C:\Users\username\AppData\Roaming\npm\node_modules`



### **命名那些事**

- 程序命名规范

  - 哪一种命名方式好

    |   修饰词在前   |   修饰词在后   |
    | :------------: | :------------: |
    |   showDialog   |   dialogShow   |
    | changePageSize | pageSizeChange |
    |   avgRevenue   |   revenueAvg   |
    |                |                |

**哪一种函数命名好**

1. `tapXxxBtn()` // 以用户的操作命名

2. `showXxxList()` // 以程序的操作命名(为了响应用户操作)，又称业务功能命名

3. 用户操作函数触发业务函数，也就是

   ```js
   tapXxxBtn() {
     showXxxList()  
   }
   ```

   

#### CSS之 BEM命名

<https://www.cnblogs.com/jianxian/p/11084305.html>

> 看看elementUI源码，vant

**什么是 BEM 命名规范**

Bem 是块（block）、元素（element）、修饰符（modifier）的简写，由 Yandex 团队提出的一种前端 CSS 命名方法论。

BEM 是一个简单又非常有用的命名约定。让你的前端代码更容易阅读和理解，更容易协作，更容易控制，更加健壮和明确，而且更加严密。

 **BEM 命名模式**

BEM 命名约定的模式是：

```
.block {}
.block__element {}
.block--modifier {}
```

- block 代表了更高级别的抽象或组件。
- block__element 代表 .block 的后代，用于形成一个完整的 .block 的整体。
- block--modifier 代表 .block 的不同状态或不同版本。

使用两个连字符和下划线而不是一个，是为了让你自己的块可以用单个连字符来界定。如：

```
.sub-block__element {}
.sub-block--modifier {}
```

**在 CSS 预处理器中使用 BEM 格式**

BEM 的一个槽点是，命名方式长而难看，书写不雅。相比 BEM 格式带来的便利来说，我们应客观看待。

而且，一般来说会使用通过 LESS/SASS 等预处理器语言来编写 CSS，利用其语言特性书写起来要简单很多。

以 sass 为例：

```scss
.article {
    max-width: 1200px;
 
    &__body {
        padding: 20px;
    }
 
    &__button {
        padding: 5px 8px;
 
        &--primary {background: blue;}
        &--success {background: green;}
    }
}
```

***注意*** 

1.  避免.block\__el1\__el2 的格式
2.  尽量让css的层级只有一层

### 对话框组件该如何使用



### **通用时间格式化方法讲解**

- 字符串`replace`方法

  ```js
  let re = /a(b)/
  let str = 'hello ab word'
  str.replace(re, function(all, arg1) {
    console.log(' all,arg1: ', all,arg1)
  })
  ```

  

### **节流和防抖**

 ![](https://user-gold-cdn.xitu.io/2019/5/16/16ac0fba56ec1726?imageslim )

- **函数防抖  debounce **

  函数防抖（debounce）：当持续触发事件时，一定时间段内没有再触发事件，事件处理函数才会执行一次，如果设定的时间到来之前，又一次触发了事件，就重新开始延时。如下图，持续触发scroll事件时，并不执行handle函数，当1000毫秒内没有触发scroll事件时，才会延时触发scroll事件 

  ***当持续触发scroll事件时，事件处理函数handle只在停止滚动1000毫秒之后才会调用一次，也就是说在持续触发scroll事件的过程中，事件处理函数handle一直没有执行。*** 

  ```js
  /*
  * 防抖
  * @param {function} fn 
  * @param {number} delay 延迟 
  */
  function debounce(fn, delay = 500) {
    let timeout = null
    return function () {
      let context = this
      let args = arguments
  
      if (timeout) clearTimeout(timeout)
  
      timeout = setTimeout(function () {
        fn.apply(context, args)
      }, delay)
    }
  }
  ```

  

- **函数节流  throttle ** 一定时间内只执行一次

  ```js
  /**
    * 节流
    * @param {function} fn 
    * @param {number} interval 间隔
    */
  function throttle(fn, interval = 500) {
    let timer = null
    return function () {
      if (timer === null) {
        let context = this
        let args = arguments
  
        timer = setTimeout(function () {
          fn.apply(context, args)
          timer = null
        }, interval)
      }
    }
  }
  ```

  

