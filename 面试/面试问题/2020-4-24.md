# 深圳市达银控股有限公司 2020/4/24

## 基本知识

##### javascript的基本数据类型

##### 谈谈对MVVM的理解

MVVM分为Model、View、ViewMode三者。

- Model：代表数据模型，数据和业务逻辑都在Model层中定义；
- View：代表UI视图，负责数据的展示；
- ViewModel：负责监听Model中数据的改变并且控制视图的更新，处理用户交互操作；

   Model和View并无直接关联，而是通过ViewModel来进行联系的，Model和ViewModel之间有着双向数据绑定的联系，因此当Model中的数据改变时会触发View层的刷新，View中由于用户交互操作而改变的数据也会在Model中同步。
这种模式实现了Model和View的数据自动同步，因此开发者只需要专注对数据的维护操作即可，而不需要自己操作dom。



##### 原型链和作用域链

**作用域**是针对变量的，比如我们、创建了一个函数，函数里面又包含了一个函数，那么现在就有三个作用域

　　全局作用域==>函数1作用域==>函数2作用域

**原型链**是针对构造函数的，比如我先创建了一个函数

##### 说说闭包

##### 闭包要注意什么问题

内存泄漏问题

##### 闭包产生内存泄漏的原因

##### **箭头函数的特点（和普通函数的区别）**

1. 箭头函数不能绑定arguments，取而代之用 rest 参数...解决
2. 箭头函数是匿名函数，不能作为构造函数，不能使用new
3. 箭头函数不绑定this，会捕获其所在的上下文的this值，作为自己的this值
4. 箭头函数没有原型属性

##### 有没有遇到过跨域，你是怎么解决跨域的

##### 为什么会产生跨域，怎么样会产生跨域

##### ES6的新特性

##### Promise解决了什么问题

##### 浏览器的事件传播机制

##### 如何防止父元素触发事件（禁止冒泡）






## Vue

vue的生命周期

##### **vue-router的路由钩子**

beforeEach

afterEach

beforeEnter

##### **vue-router的组件内的路由钩子**

beforeRouterEnter

beforeRouteUpdate

beforeRouteLeave

##### vue事件传递的几种方法



