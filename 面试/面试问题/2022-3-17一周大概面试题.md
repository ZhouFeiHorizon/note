### 迈克尔斯项目

1. 自我介绍

2. 你刚才说你在学校表现优秀，怎么个优秀， 那我问你，你觉得哪门课程学的比较好

   我：C语言，基础的东西，给我未来打下了基础

   面试官：那你说说C语言的一些东西，C语音的一些问题

   我：。。。，我是来面试前端的

   面试官：我知道，但我们还是要聊一聊

3. vue `v-clock` 的作用，是用来干嘛的

4. `Vue`的一个响应式原理

5. 怎么让 `a == 1 && a == 2 && a == 3 `这个表达式结果为`true`

   我：三种方式解决方案

6. 页面很卡，渲染一千条数据你会怎么做

7. 前端方面的性能优化

8. 括号匹配

   ```js
   /**
    * @param {string} s
    * @return {boolean}
    */
   var isValid = function (s) {
     // 你的代码
   }
   console.log(isValid("([])")) // true
   console.log(isValid("()[]({})")) // true
   console.log(isValid("([)]")) // false
   ```





页面上有一个按钮、要求鼠标永远点击不到他



1. 实现一个九宫格布局 

   > 要求： 只能这样放，一个盒子里面放9个div, 中间的宽度是两边盒子之和，不能给那9个盒子设置类名

2.  `map` 和 `set` 的区别

3. 打印结果是什么，为什么会这样

   ```js
   console.log(a) 
   var a = 1
   console.log(' a: ', a) 
   ```

   那把 `var` 缓存 `let` 会是打印什么

4. 常见的报错有哪些

5. 说说原型、原型链、`var`和`let`、`const`的区别

6. 异步解决方案有哪些

7. 所说`Promise`, `Promise.all`的用法

8. 如何实现，我现在有10个请求，我现在要想，最多只能同时有三个请求去请求

9. 如何解决跨越

10. 代码实现节流

11. 说说你对`axios`的一些封装，拦截做了些什么

12.  说结果 `[1]===[1]` `[1]==[1]` `[1] == '1'`



事件循环机制

写一个发布者订阅者模式

### 框架Vue、React

1. `vue`组件之间的通信方式
2. `vue key`值的作用
3. `vue` 响应式原理
4. 说说`vue diff`
5. 如何实现一个 `v-model`
6. 说说`keep-alive`的作用， 解决了哪些问题，场景
7. `vue-router` 的路由模式
8. 说说`Vuex`（介绍用法就可以了）
9. `Vue3`比`Vue2`好的地方
10. 组合式API对比选项式API的优点
11. 说说Reat和Vue的优缺点

### 工程化/项目

1. `git` 的常用命令 `git pull`和 `git fetch`的区别
2. `webpack`常用`plugin`和`loader`、`plugin`和`loader`的区别
3. `webpack`的一个编译过程
4. 说说`AST`
5. `babel`的一个工作流程
6. 项目中的一个工作流程是怎么样的，怎么自动化构建
7. 做了项目哪些优化、`SSR`服务端渲染解决了什么问题
8. 登录权限是如何做的、如何做权限管理

### 浏览器/网络

1. 强缓存、协商缓存
2. 从URL输入到页面渲染发生了什么
3. 如何调试内存泄漏、白屏时间